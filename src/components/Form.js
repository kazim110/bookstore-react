import React from 'react';

const Form = () => (
  <form>
    <input type="text" placeholder="Enter book's name..." />
    <input type="text" placeholder="Enter book's author..." />
    <button type="submit">Add book</button>
  </form>
);

export default Form;
